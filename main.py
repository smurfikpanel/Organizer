"""Точка входу для хостингу FPS.ms (та подібних сервісів, які за замовчуванням
шукають окремий стартовий файл у корені проєкту). Просто запускає ту саму
логіку, що й локальний запуск через `python -m app.bot` — жодних змін у самому
боті. Ім'я файлу навмисно НЕ 'app.py', щоб уникнути конфлікту назв із пакетом
app/ (де лежить весь код бота) — на FPS.ms достатньо в Startup-налаштуваннях
вказати, що стартовий файл 'main.py'."""

import asyncio

from app.bot import main

if __name__ == "__main__":
    asyncio.run(main())
